"use client";import Link from "next/link";import { useActionState } from "react";import { PRIORITIES,TASK_STATUS } from "@/lib/constants";import { Button,buttonVariants } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Checkbox,Field,Input,Select,Textarea } from "@/components/ui/input";import type { TaskActionResult } from "./actions";
type Option={id:string;name?:string;fullName?:string;position?:string|null};export type TaskDefaults={title:string;description:string;projectId:string;responsibleId:string;participantIds:string[];priority:string;status:string;startDate:string;dueDate:string;estimatedHours:string;tagNames:string;dependsOnId:string;blockReason:string};export const EMPTY_TASK:TaskDefaults={title:"",description:"",projectId:"",responsibleId:"",participantIds:[],priority:"MEDIUM",status:"NOT_STARTED",startDate:"",dueDate:"",estimatedHours:"",tagNames:"",dependsOnId:"",blockReason:""};
export function TaskForm({action,projects,users,tasks,defaults=EMPTY_TASK,editing=false}:{action:(p:TaskActionResult|null,d:FormData)=>Promise<TaskActionResult>;projects:Option[];users:Option[];tasks:Option[];defaults?:TaskDefaults;editing?:boolean}){const[state,formAction,pending]=useActionState(action,null);const e=state?.fieldErrors??{};return <form action={formAction} className="space-y-6">{state?.message&&!state.ok&&<p role="alert" className="rounded-lg border border-danger/30 bg-danger/10 p-3 text-sm text-danger">{state.message}</p>}<Card><CardHeader><CardTitle>Dados da tarefa</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><Field className="sm:col-span-2" label="Título" htmlFor="title" required error={e.title}><Input id="title" name="title" required maxLength={200} defaultValue={defaults.title}/></Field><Field className="sm:col-span-2" label="Descrição" htmlFor="description" error={e.description}><Textarea id="description" name="description" defaultValue={defaults.description}/></Field><Field label="Projeto" htmlFor="projectId" error={e.projectId}><Select id="projectId" name="projectId" defaultValue={defaults.projectId}><option value="">Sem projeto</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</Select></Field><Field label="Responsável" htmlFor="responsibleId" error={e.responsibleId}><Select id="responsibleId" name="responsibleId" defaultValue={defaults.responsibleId}><option value="">Sem responsável</option>{users.map(u=><option key={u.id} value={u.id}>{u.fullName}{u.position?` — ${u.position}`:""}</option>)}</Select></Field><Field label="Prioridade" htmlFor="priority" error={e.priority}><Select id="priority" name="priority" defaultValue={defaults.priority}>{Object.entries(PRIORITIES).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select></Field><Field label="Status" htmlFor="status" error={e.status}><Select id="status" name="status" defaultValue={defaults.status}>{Object.entries(TASK_STATUS).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select></Field><Field label="Início" htmlFor="startDate" error={e.startDate}><Input id="startDate" name="startDate" type="date" defaultValue={defaults.startDate}/></Field><Field label="Prazo" htmlFor="dueDate" error={e.dueDate}><Input id="dueDate" name="dueDate" type="date" defaultValue={defaults.dueDate}/></Field><Field label="Horas estimadas" htmlFor="estimatedHours" error={e.estimatedHours}><Input id="estimatedHours" name="estimatedHours" inputMode="decimal" defaultValue={defaults.estimatedHours}/></Field><Field label="Depende da tarefa" htmlFor="dependsOnId" error={e.dependsOnId}><Select id="dependsOnId" name="dependsOnId" defaultValue={defaults.dependsOnId}><option value="">Nenhuma</option>{tasks.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</Select></Field><Field className="sm:col-span-2" label="Tags (separadas por vírgula)" htmlFor="tagNames" error={e.tagNames}><Input id="tagNames" name="tagNames" defaultValue={defaults.tagNames}/></Field><Field className="sm:col-span-2" label="Motivo do bloqueio" htmlFor="blockReason" error={e.blockReason}><Textarea id="blockReason" name="blockReason" defaultValue={defaults.blockReason}/></Field></CardContent></Card><Card><CardHeader><CardTitle>Participantes</CardTitle></CardHeader><CardContent><div className="grid gap-2 sm:grid-cols-2">{users.map(u=><label key={u.id} className="flex cursor-pointer items-center gap-2 rounded-lg p-2 hover:bg-card-hover"><Checkbox name="participantIds" value={u.id} defaultChecked={defaults.participantIds.includes(u.id)}/><span className="text-sm">{u.fullName}</span></label>)}</div></CardContent></Card><div className="flex justify-end gap-2"><Link href="/tarefas" className={buttonVariants({variant:"secondary"})}>Cancelar</Link><Button type="submit" loading={pending}>{editing?"Salvar tarefa":"Criar tarefa"}</Button></div></form>}
