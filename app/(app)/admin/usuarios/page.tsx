import Link from "next/link";import { Pencil,Plus } from "lucide-react";import type { Prisma } from "@prisma/client";import { db } from "@/lib/db";import { requireRole } from "@/lib/auth/current-user";import { PageHeader } from "@/components/layout/page-header";import { Table,TBody,TD,TH,THead,TR } from "@/components/ui/table";import { Button,buttonVariants } from "@/components/ui/button";import { Input,Select } from "@/components/ui/input";import { USER_ROLES,USER_STATUS } from "@/lib/constants";import { RemoveUserButton } from "./remove-user-button";
export default async function AdminUsersPage({searchParams}:{searchParams:Promise<{q?:string;papel?:string;status?:string}>}){const current=await requireRole("ADMIN");const sp=await searchParams;const q=(sp.q??"").trim();const role=sp.papel&&sp.papel in USER_ROLES?sp.papel:"";const status=sp.status&&sp.status in USER_STATUS?sp.status:"";const where:Prisma.UserWhereInput={deletedAt:null,...(role?{role}:{}),...(status?{status}:{}),...(q?{OR:[{fullName:{contains:q,mode:"insensitive"}},{username:{contains:q,mode:"insensitive"}},{email:{contains:q,mode:"insensitive"}},{position:{contains:q,mode:"insensitive"}}]}:{})};const users=await db.user.findMany({where,orderBy:[{status:"asc"},{createdAt:"desc"}]});return <><PageHeader title="Administração de usuários" description="Crie acessos, defina cargos e controle as permissões." actions={<Link href="/admin/usuarios/novo" className={buttonVariants()}><Plus className="h-4 w-4"/>Novo usuário</Link>}/><form className="mb-5 grid gap-3 rounded-xl border border-border bg-card p-3 sm:grid-cols-[1fr_200px_180px_auto]"><Input name="q" defaultValue={q} placeholder="Nome, login, e-mail ou cargo"/><Select name="papel" defaultValue={role}><option value="">Todos os papéis</option>{Object.entries(USER_ROLES).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select><Select name="status" defaultValue={status}><option value="">Todos os status</option>{Object.entries(USER_STATUS).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select><Button variant="secondary">Filtrar</Button></form><Table><THead><TR><TH>Usuário</TH><TH>Cargo / área</TH><TH>Papel</TH><TH>Status</TH><TH>Ações</TH></TR></THead><TBody>{users.map(u=><TR key={u.id}><TD><p className="font-medium">{u.fullName}</p><p className="text-xs text-muted">{u.email} · @{u.username}</p></TD><TD><p>{u.position??"Sem cargo"}</p><p className="text-xs text-muted">{u.area??"Sem área"}</p></TD><TD>{USER_ROLES[u.role as keyof typeof USER_ROLES]??u.role}</TD><TD>{USER_STATUS[u.status as keyof typeof USER_STATUS]??u.status}</TD><TD><div className="flex gap-2"><Link aria-label={`Editar ${u.fullName}`} href={`/admin/usuarios/${u.id}/editar`} className={buttonVariants({variant:"secondary",size:"iconSm"})}><Pencil className="h-4 w-4"/></Link>{u.id!==current.id&&<RemoveUserButton id={u.id} name={u.fullName}/>}</div></TD></TR>)}</TBody></Table></>}
