"use client";import Link from "next/link";import { useActionState } from "react";import { MISSION_STATUS,PRIORITIES } from "@/lib/constants";import { Button,buttonVariants } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Field,Input,Select,Textarea } from "@/components/ui/input";import type { ObjectiveActionResult } from "@/app/(app)/metas/actions";
export type MissionDefaults={title:string;description:string;priority:string;status:string;points:string;assigneeId:string;goalId:string;startDate:string;dueDate:string};export const EMPTY_MISSION:MissionDefaults={title:"",description:"",priority:"MEDIUM",status:"NOT_STARTED",points:"0",assigneeId:"",goalId:"",startDate:"",dueDate:""};
export function MissionForm({action,users,goals,defaults=EMPTY_MISSION,editing=false}:{action:(p:ObjectiveActionResult|null,d:FormData)=>Promise<ObjectiveActionResult>;users:{id:string;fullName:string}[];goals:{id:string;title:string}[];defaults?:MissionDefaults;editing?:boolean}){const[state,formAction,pending]=useActionState(action,null);const e=state?.fieldErrors??{};return <form action={formAction} className="space-y-6">{state?.message&&!state.ok&&<p className="rounded-lg border border-danger/30 bg-danger/10 p-3 text-sm text-danger">{state.message}</p>}<Card><CardHeader><CardTitle>Informações da missão</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2"><Field className="sm:col-span-2" label="Título" htmlFor="title" required error={e.title}><Input id="title" name="title" defaultValue={defaults.title} required/></Field><Field className="sm:col-span-2" label="Descrição" htmlFor="description" error={e.description}><Textarea id="description" name="description" defaultValue={defaults.description}/></Field><Field label="Responsável" htmlFor="assigneeId" error={e.assigneeId}><Select id="assigneeId" name="assigneeId" defaultValue={defaults.assigneeId}><option value="">Sem responsável</option>{users.map(u=><option key={u.id} value={u.id}>{u.fullName}</option>)}</Select></Field><Field label="Meta vinculada" htmlFor="goalId" error={e.goalId}><Select id="goalId" name="goalId" defaultValue={defaults.goalId}><option value="">Sem meta</option>{goals.map(g=><option key={g.id} value={g.id}>{g.title}</option>)}</Select></Field><Field label="Prioridade" htmlFor="priority" error={e.priority}><Select id="priority" name="priority" defaultValue={defaults.priority}>{Object.entries(PRIORITIES).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select></Field><Field label="Status" htmlFor="status" error={e.status}><Select id="status" name="status" defaultValue={defaults.status}>{Object.entries(MISSION_STATUS).map(([k,v])=><option key={k} value={k}>{v}</option>)}</Select></Field><Field label="Pontos" htmlFor="points" error={e.points}><Input id="points" name="points" type="number" min="0" defaultValue={defaults.points}/></Field><div/><Field label="Início" htmlFor="startDate" error={e.startDate}><Input id="startDate" name="startDate" type="date" defaultValue={defaults.startDate}/></Field><Field label="Prazo" htmlFor="dueDate" error={e.dueDate}><Input id="dueDate" name="dueDate" type="date" defaultValue={defaults.dueDate}/></Field></CardContent></Card><div className="flex justify-end gap-2"><Link href="/missoes" className={buttonVariants({variant:"secondary"})}>Cancelar</Link><Button loading={pending}>{editing?"Salvar missão":"Criar missão"}</Button></div></form>}
