import { db } from "@/lib/db";import { requireRole } from "@/lib/auth/current-user";import { PageHeader } from "@/components/layout/page-header";import { createGoalAction } from "../actions";import { GoalForm } from "../goal-form";
export default async function NewGoalPage(){await requireRole("ADMIN","MANAGER");const users=await db.user.findMany({where:{status:"ACTIVE",deletedAt:null,role:{not:"CLIENT"}},select:{id:true,fullName:true},orderBy:{fullName:"asc"}});return <div className="mx-auto max-w-3xl"><PageHeader title="Nova meta" description="Defina o indicador, o valor alvo e o responsável."/><GoalForm action={createGoalAction} users={users}/></div>}
